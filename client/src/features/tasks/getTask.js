import React from 'react';
function getTask() {
    // const { token } = useContext(AuthContext)
    // const [ getTask ] = useMutation(GET_TASK, {
    //     context: {
    //         headers: {
    //             authorization: `Bearer ${token}`
    //         }
    //     }
    // })
    return (React.createElement("div", null, "getTask"));
}
export default getTask;
